# Benchmark figures

Drop benchmark comparison figures here (PNG/SVG) and uncomment the corresponding
`![...](figures/...)` lines in the benchmark pages. One subject per file, named
`<benchmark>_<subject>.png` (e.g. `love_elastic.png`, `disc_uplift.png`,
`sle_E2_fig10.png`).
